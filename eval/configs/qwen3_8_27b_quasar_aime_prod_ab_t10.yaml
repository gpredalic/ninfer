schema_version: 1

# AIME A/B at Qwen's official thinking temperature (1.0), production server
# configuration (QUASAR nvfp4, kv nvfp4, YaRN 2.12, 555k ctx, c=3).
#
#   t10_uniform       - post_thinking mirrors the thinking sampler (1.0), so one
#                       preset governs the whole generation. This is the
#                       pre-feature behavior and matches the recorded
#                       model-card conditions (temperature 1.0).
#   t10_postthinking - no post_thinking override, so the registered preset
#                       (temperature 0.2, top_p 0.95, top_k 20) governs the
#                       phase after the reasoning block closes.

targets:
  qwen38_api:
    protocol: openai_chat
    base_url: http://127.0.0.1:18080/v1
    model: qwen3.8-27b
    max_concurrency: 3
    request:
      timeout_seconds: 86400
      retries: 2
      retry_interval_seconds: 10

suites:
  t10_uniform:
    jobs:
      - id: aime25
        backend: evalscope
        dataset: aime25
        target: qwen38_api
        max_concurrency: 3
        generation: &t10_uniform_generation
          temperature: 1.0
          top_p: 0.95
          top_k: 20
          presence_penalty: 0.0
          frequency_penalty: 0.0
          max_tokens: 122880
          seed: 42
          extra_body:
            enable_thinking: true
            min_p: 0.0
            post_thinking:
              temperature: 1.0
              top_p: 0.95
              top_k: 20
              presence_penalty: 0.0
              frequency_penalty: 0.0
        backend_args: &aime_backend
          judge_strategy: rule
          dataset_hub: modelscope
      - id: aime26
        backend: evalscope
        dataset: aime26
        target: qwen38_api
        max_concurrency: 3
        generation: *t10_uniform_generation
        backend_args: *aime_backend

  t10_postthinking:
    jobs:
      - id: aime25
        backend: evalscope
        dataset: aime25
        target: qwen38_api
        max_concurrency: 3
        generation: &t10_postthinking_generation
          temperature: 1.0
          top_p: 0.95
          top_k: 20
          presence_penalty: 0.0
          frequency_penalty: 0.0
          max_tokens: 122880
          seed: 42
          extra_body:
            enable_thinking: true
            min_p: 0.0
        backend_args: *aime_backend
      - id: aime26
        backend: evalscope
        dataset: aime26
        target: qwen38_api
        max_concurrency: 3
        generation: *t10_postthinking_generation
        backend_args: *aime_backend

runtime:
  max_parallel_jobs: 1
  runs_dir: eval/runs
  progress:
    enabled: true
    refresh_seconds: 1
    heartbeat_seconds: 900
  samples:
    retention: all
