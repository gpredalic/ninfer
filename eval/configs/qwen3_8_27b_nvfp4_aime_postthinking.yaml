schema_version: 1

# AIME quality gate for the post-thinking sampler (commit dd5534a5).
# Two suites over the same artifact and server run:
#   aime_baseline    - post_thinking explicitly mirrors the thinking-phase params,
#                      reproducing the pre-feature single-preset sampling.
#   aime_postthinking - no post_thinking override; the registered preset
#                      (temperature 0.2, top_p 0.95, top_k 20) takes over after the
#                      model closes its reasoning block.
# Generation settings otherwise match qwen3_8_27b_nvfp4_reasoning.yaml exactly
# (temperature 1.0, top_p 0.95, top_k 20, presence 0.0, frequency 0.0,
# max_tokens 122880, seed 42, thinking on).

targets:
  qwen38_api:
    protocol: openai_chat
    base_url: http://127.0.0.1:18080/v1
    model: qwen3.8-27b
    max_concurrency: 2
    request:
      timeout_seconds: 86400
      retries: 2
      retry_interval_seconds: 10

suites:
  aime_baseline:
    jobs:
      - id: aime25
        backend: evalscope
        dataset: aime25
        target: qwen38_api
        max_concurrency: 2
        generation: &aime_generation
          temperature: 1.0
          top_p: 0.95
          top_k: 20
          presence_penalty: 0.0
          frequency_penalty: 0.0
          max_tokens: 122880
          seed: 42
          extra_body:
            enable_thinking: true
            min_p: 0.0
            post_thinking:
              temperature: 1.0
              top_p: 0.95
              top_k: 20
              presence_penalty: 0.0
              frequency_penalty: 0.0
        backend_args: &aime_backend
          judge_strategy: rule
          dataset_hub: modelscope
      - id: aime26
        backend: evalscope
        dataset: aime26
        target: qwen38_api
        max_concurrency: 2
        generation: *aime_generation
        backend_args: *aime_backend

  aime_postthinking:
    jobs:
      - id: aime25
        backend: evalscope
        dataset: aime25
        target: qwen38_api
        max_concurrency: 2
        generation: &post_generation
          temperature: 1.0
          top_p: 0.95
          top_k: 20
          presence_penalty: 0.0
          frequency_penalty: 0.0
          max_tokens: 122880
          seed: 42
          extra_body:
            enable_thinking: true
            min_p: 0.0
        backend_args: *aime_backend
      - id: aime26
        backend: evalscope
        dataset: aime26
        target: qwen38_api
        max_concurrency: 2
        generation: *post_generation
        backend_args: *aime_backend

runtime:
  max_parallel_jobs: 1
  runs_dir: eval/runs
  progress:
    enabled: true
    refresh_seconds: 1
    heartbeat_seconds: 900
  samples:
    retention: all
