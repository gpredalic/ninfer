schema_version: 1

# AIME quality gate for the post-thinking sampler.
#
# Runs the CURRENT build with the PRODUCTION server configuration on the
# production QUASAR artifact:
#   quasar nvfp4 weights, kv-dtype nvfp4, YaRN factor 2.12 (262144 native),
#   555k max context, c=3, vision, froggeric chat template.
#
# Two suites over the same artifact and the same server process:
#   without_post_thinking - post_thinking explicitly mirrors the thinking
#                           sampler, so one preset governs the whole
#                           generation (the pre-feature behavior).
#   with_post_thinking    - no post_thinking override; the registered preset
#                           (temperature 0.2, top_p 0.95, top_k 20) takes over
#                           after the model closes its reasoning block.
#
# Generation settings match the production defaults (temperature 0.9,
# top_p 0.95, top_k 20) with thinking enabled.

targets:
  qwen38_api:
    protocol: openai_chat
    base_url: http://127.0.0.1:18080/v1
    model: qwen3.8-27b
    max_concurrency: 3
    request:
      timeout_seconds: 86400
      retries: 2
      retry_interval_seconds: 10

suites:
  without_post_thinking:
    jobs:
      - id: aime25
        backend: evalscope
        dataset: aime25
        target: qwen38_api
        max_concurrency: 3
        generation: &uniform_generation
          temperature: 0.9
          top_p: 0.95
          top_k: 20
          presence_penalty: 0.0
          frequency_penalty: 0.0
          max_tokens: 122880
          seed: 42
          extra_body:
            enable_thinking: true
            min_p: 0.0
            # Mirror the thinking sampler so the entire generation uses one
            # preset, reproducing the build before the post-thinking feature.
            post_thinking:
              temperature: 0.9
              top_p: 0.95
              top_k: 20
              presence_penalty: 0.0
              frequency_penalty: 0.0
        backend_args: &aime_backend
          judge_strategy: rule
          dataset_hub: modelscope
      - id: aime26
        backend: evalscope
        dataset: aime26
        target: qwen38_api
        max_concurrency: 3
        generation: *uniform_generation
        backend_args: *aime_backend

  with_post_thinking:
    jobs:
      - id: aime25
        backend: evalscope
        dataset: aime25
        target: qwen38_api
        max_concurrency: 3
        generation: &postthinking_generation
          temperature: 0.9
          top_p: 0.95
          top_k: 20
          presence_penalty: 0.0
          frequency_penalty: 0.0
          max_tokens: 122880
          seed: 42
          extra_body:
            enable_thinking: true
            min_p: 0.0
        backend_args: *aime_backend
      - id: aime26
        backend: evalscope
        dataset: aime26
        target: qwen38_api
        max_concurrency: 3
        generation: *postthinking_generation
        backend_args: *aime_backend

runtime:
  max_parallel_jobs: 1
  runs_dir: eval/runs
  progress:
    enabled: true
    refresh_seconds: 1
    heartbeat_seconds: 900
  samples:
    retention: all
