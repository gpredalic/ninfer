schema_version: 1

# ReSET-style test: does a COLD emission phase help a benchmark whose answer is a
# low-entropy symbolic token (an AIME integer)? Thinking stays at 1.0 in every arm, so
# only the tokens after the reasoning block are affected. Arms differ ONLY in post_thinking.

targets:
  qwen38_api:
    protocol: openai_chat
    base_url: http://127.0.0.1:18090/v1
    model: qwen3.8-27b
    max_concurrency: 2
    request:
      timeout_seconds: 86400
      retries: 2
      retry_interval_seconds: 10

suites:
  emit_00:
    jobs:
      - id: aime25
        backend: evalscope
        dataset: aime25
        target: qwen38_api
        max_concurrency: 2
        generation: &genemit_00
          temperature: 1.0
          top_p: 0.95
          top_k: 20
          presence_penalty: 0.0
          frequency_penalty: 0.0
          max_tokens: 122880
          seed: 42
          extra_body:
            enable_thinking: true
            min_p: 0.0
            post_thinking:
              temperature: 0.0
              top_p: 1.0
              top_k: 20
              presence_penalty: 0.0
              frequency_penalty: 0.0
        backend_args: {judge_strategy: rule, dataset_hub: modelscope}
      - id: aime26
        backend: evalscope
        dataset: aime26
        target: qwen38_api
        max_concurrency: 2
        generation: *genemit_00
        backend_args: {judge_strategy: rule, dataset_hub: modelscope}

  emit_02:
    jobs:
      - id: aime25
        backend: evalscope
        dataset: aime25
        target: qwen38_api
        max_concurrency: 2
        generation: &genemit_02
          temperature: 1.0
          top_p: 0.95
          top_k: 20
          presence_penalty: 0.0
          frequency_penalty: 0.0
          max_tokens: 122880
          seed: 42
          extra_body:
            enable_thinking: true
            min_p: 0.0
            post_thinking:
              temperature: 0.2
              top_p: 0.95
              top_k: 20
              presence_penalty: 0.0
              frequency_penalty: 0.0
        backend_args: {judge_strategy: rule, dataset_hub: modelscope}
      - id: aime26
        backend: evalscope
        dataset: aime26
        target: qwen38_api
        max_concurrency: 2
        generation: *genemit_02
        backend_args: {judge_strategy: rule, dataset_hub: modelscope}

  emit_10:
    jobs:
      - id: aime25
        backend: evalscope
        dataset: aime25
        target: qwen38_api
        max_concurrency: 2
        generation: &genemit_10
          temperature: 1.0
          top_p: 0.95
          top_k: 20
          presence_penalty: 0.0
          frequency_penalty: 0.0
          max_tokens: 122880
          seed: 42
          extra_body:
            enable_thinking: true
            min_p: 0.0
            post_thinking:
              temperature: 1.0
              top_p: 0.95
              top_k: 20
              presence_penalty: 0.0
              frequency_penalty: 0.0
        backend_args: {judge_strategy: rule, dataset_hub: modelscope}
      - id: aime26
        backend: evalscope
        dataset: aime26
        target: qwen38_api
        max_concurrency: 2
        generation: *genemit_10
        backend_args: {judge_strategy: rule, dataset_hub: modelscope}

runtime:
  max_parallel_jobs: 1
  runs_dir: eval/runs
  progress: {enabled: true, refresh_seconds: 1, heartbeat_seconds: 900}
  samples: {retention: all}
